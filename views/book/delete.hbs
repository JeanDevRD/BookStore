<div class="row mb-3">
    <h1><i class="bi bi-trash3"></i> Eliminar Libro</h1>
    <hr />
</div>

<div class="row d-flex justify-content-center">
    <div class="col-12 col-sm-6">
        <div class="card shadow-sm border-danger">
            <div class="card-body text-center">
                <p class="fs-5">¿Está seguro que desea eliminar este libro?</p>
                <p class="fw-bold fs-4">{{book.title}}</p>
                <p class="text-muted small"><i class="bi bi-person me-1"></i>{{book.Author.name}}</p>
                <div class="d-flex gap-3 justify-content-center mt-4">
                    <a href="/books/index" class="btn btn-secondary">
                        <i class="bi bi-x-circle"></i> Cancelar
                    </a>
                    <form method="POST" action="/books/delete">
                        <input type="hidden" name="bookId" value="{{book.id}}" />
                        <button type="submit" class="btn btn-danger">
                            <i class="bi bi-trash3"></i> Eliminar
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>